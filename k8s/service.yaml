apiVersion: v1
kind: Service
metadata:
  name: cloupilot
  namespace: cloupilot
  labels:
    app: cloupilot
spec:
  selector:
    app: cloupilot
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
